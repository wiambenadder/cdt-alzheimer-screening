name: cdt-alz
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch>=2.1
  - torchvision>=0.16
  - pytorch-cuda=12.1      # change to match your specific CUDA and remove line on CPU-only
  - numpy>=1.24
  - pandas>=2.0
  - pillow>=10.0
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - seaborn>=0.12
  - tqdm>=4.65
  - tensorboard>=2.14
  - jupyterlab>=4.0
  - ipywidgets>=8.0
